apiVersion: v1
kind: Service
metadata:
  name: {{ include "warp-syncer.fullname" . }}
  labels:
    {{- include "warp-syncer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "warp-syncer.selectorLabels" . | nindent 4 }}

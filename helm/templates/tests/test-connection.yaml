apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "warp-syncer.fullname" . }}-test-connection"
  labels:
    {{- include "warp-syncer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "warp-syncer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
